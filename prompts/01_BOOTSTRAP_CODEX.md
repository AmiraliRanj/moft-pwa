# Prompt 01: Bootstrap and complete the preview

Use the repository instructions and the `$moft-product-builder` skill.

Inspect the whole starter before editing. Run the project, identify broken or incomplete behavior, and turn it into a polished university-demo PWA for «مفت».

Requirements:

- Preserve Persian RTL as the primary interface.
- Preserve the Apple-inspired but independent brand direction.
- Keep the preview mock-only: no real payments, auth, or database.
- Ensure category filters, search, offer details, reservation simulation, local persistence, install CTA, offline page, and all four tabs work.
- Make the mobile experience around 390px the priority, then ensure desktop looks intentional.
- Avoid unnecessary dependencies.
- Run `npm run validate`, `npm run lint`, and `npm run build`.
- Fix every issue found before stopping.
- Summarize changed files, checks run, and any remaining limitations.
