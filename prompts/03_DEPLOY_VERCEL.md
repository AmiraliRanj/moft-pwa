# Prompt 03: Vercel release

Use `$pwa-vercel-release`.

Prepare this repository for a Vercel preview deployment. Verify the production build, PWA manifest, icons, service worker registration, offline fallback, metadata, and README deployment instructions.

Do not deploy with `--prod` unless I explicitly ask. Report the exact command I should run and any Vercel dashboard settings required.
