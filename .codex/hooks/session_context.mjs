import { readInput, output } from "./_io.mjs";

await readInput();
output({
  hookSpecificOutput: {
    hookEventName: "SessionStart",
    additionalContext: "For this repository, read AGENTS.md first. The product is a Persian RTL mock-only PWA named مفت. Preserve pickup windows, surprise-box uncertainty, food-safety wording, independent Apple-inspired styling, and Vercel compatibility."
  }
});
